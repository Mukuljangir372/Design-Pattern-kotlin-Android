package com.mu.jan.myapplication.structuralpattern

/**
 * Facade Pattern,
 * In Facade Pattern, In simple words, while working with data,
 * we don't know, from where and How data is coming.
 *
 *
 * val list = apiService.getMoviesList()
 *
 * In this, we don't know How movies list fetched?, How Json converted
 * into java object?
 */
